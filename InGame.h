#include "Board.h"
#include <ctime>
#include <algorithm>
#include <random>

void CreateMatrix(int);
//bool CheckPath(int, int, int, int, int, int, int, char);

struct Classic
{
    static void Easy();
};